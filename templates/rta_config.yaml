# RTA Project Configuration
# Generated by: project_setup tool
# Purpose: Single source of truth for methodology and project state

project:
  name: "{{PROJECT_NAME}}"
  researcher: "{{RESEARCHER}}"
  created: "{{CREATED}}"
  last_updated: "{{CREATED}}"

methodology:
  # General documents (loaded at project start)
  general:
    - rta_overview.md
    - KODNINGSMANUAL_AI_Augmented_RTA_DISRUPTIV_INTEGRATED_2026-01-10.md

  # Phase-specific documents
  # Claude reads these dynamically based on current phase
  phases:
    phase1:
      - phase1_familiarization.md
    phase2a:
      - phase2a_initial_coding.md
      - KODNINGSMANUAL_AI_Augmented_RTA_DISRUPTIV_INTEGRATED_2026-01-10.md
    phase2b:
      - phase2b_critical_review.md
    phase3:
      - phase3_generating_themes.md
    phase4:
      - phase4_reviewing_themes.md
    phase5:
      - phase5_defining_naming.md
    phase6:
      - phase6_producing_report.md

  # Epistemology (loaded at project start + on-demand)
  epistemology:
    load_at_start: true
    documents:
      - epistemology/constructionist.md
      - epistemology/inductive_deductive.md
      - epistemology/semantic_latent.md

# Transcripts to analyze (COPIED to project - originals never modified)
transcripts: []
  # Populated by project_setup:
  # - path: "/project/transcripts/file.md"        # COPIED file (work on this)
  #   original_path: "/original/location/file.md" # Original (preserved)
  #   name: "file.md"
  #   status: "pending"
  #   current_phase: null

# State tracking
state:
  current_phase: null
  started_at: null
